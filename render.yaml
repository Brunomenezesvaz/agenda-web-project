services:
  # Backend - Java Spring Boot API
  - type: web
    name: agenda-backend-joao
    runtime: docker
    rootDirectory: backend
    dockerContext: backend
    dockerfilePath: Dockerfile
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: agenda-db-joao
          property: host
      - key: DB_PORT
        fromDatabase:
          name: agenda-db-joao
          property: port
      - key: DB_NAME
        fromDatabase:
          name: agenda-db-joao
          property: database
      - key: DB_USER
        fromDatabase:
          name: agenda-db-joao
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: agenda-db-joao
          property: password

  # Frontend - React Application
  - type: web
    name: agenda-frontend-joao
    runtime: docker
    rootDirectory: frontend
    dockerContext: frontend
    dockerfilePath: Dockerfile
    envVars:
      - key: REACT_APP_API_URL
        value: https://agenda-backend-joao.onrender.com/api

databases:
  - name: agenda-db-joao
